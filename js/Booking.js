class Booking {
	constructor(stationNumber, stationAddress, lastName, firstName) {
		this.stationNumber = stationNumber;
		this.stationAddress = stationAddress;
		this.lastName = lastName;
		this.firstName = firstName;
	}
}